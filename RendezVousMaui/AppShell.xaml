<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="RendezVousMaui.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:RendezVousMaui.Views">

    <TabBar>

        <!-- Onglet Clients -->
        <ShellContent
            Title="Clients"
            ContentTemplate="{DataTemplate views:ClientsPage}" />

        <!-- Onglet Rendez-vous -->
        <ShellContent
            Title="Rendez-vous"
            ContentTemplate="{DataTemplate views:AppointmentsPage}" />

        <!-- Onglet Agenda / Créneaux -->
        <ShellContent
            Title="Agenda"
            ContentTemplate="{DataTemplate views:AgendaPage}" />

    </TabBar>

</Shell>
